<Window x:Class="UI.ReaderWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:UI"
        mc:Ignorable="d"
        Title="Читатели библиотеки" Height="600" Width="900"
        WindowStartupLocation="CenterScreen">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Список читателей" 
                   FontSize="20" FontWeight="Bold" 
                   HorizontalAlignment="Center" 
                   Margin="0,0,0,20"/>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,15">
            <TextBlock Text="Поиск:" 
                       VerticalAlignment="Center" 
                       Margin="0,0,10,0"/>
            <TextBox x:Name="SearchTextBox" 
                     Width="200" 
                     Height="25"
                     Padding="5"
                     TextChanged="SearchTextBox_TextChanged"/>
            <Button x:Name="SearchButton" 
                    Content="Поиск" 
                    Margin="10,0,0,0"
                    Padding="10,5"
                    Click="SearchButton_Click"/>
            <Button x:Name="ResetButton" 
                    Content="Сброс" 
                    Margin="10,0,0,0"
                    Padding="10,5"
                    Click="ResetButton_Click"/>
        </StackPanel>

        <DataGrid x:Name="ReadersDataGrid" 
                  Grid.Row="2" 
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  CanUserDeleteRows="False"
                  SelectionMode="Single"
                  SelectionChanged="ReadersDataGrid_SelectionChanged">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" 
                                    Binding="{Binding Id}" 
                                    Width="50"/>
                <DataGridTextColumn Header="ФИО" 
                                    Binding="{Binding FullName}" 
                                    Width="200"/>
                <DataGridTextColumn Header="Адрес" 
                                    Binding="{Binding Address}" 
                                    Width="150"/>
                <DataGridTextColumn Header="Телефон" 
                                    Binding="{Binding PhoneNumber}" 
                                    Width="120"/>
                <DataGridTextColumn Header="Email" 
                                    Binding="{Binding Email}" 
                                    Width="150"/>
                <DataGridTextColumn Header="Категория" 
                                    Binding="{Binding ReaderCategory.Denomination}" 
                                    Width="100"/>
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Grid.Row="3" 
                    Orientation="Horizontal" 
                    HorizontalAlignment="Right" 
                    Margin="0,15,0,0">

            <Button x:Name="AddReaderButton" 
                    Content="Добавить читателя" 
                    Padding="15,5"
                    Margin="0,0,10,0"
                    Click="AddReaderButton_Click"/>

            <Button x:Name="EditReaderButton" 
                    Content="Редактировать" 
                    Padding="15,5"
                    Margin="0,0,10,0"
                    Click="EditReaderButton_Click"
                    IsEnabled="False"/>

            <Button x:Name="DeleteReaderButton" 
                    Content="Удалить" 
                    Padding="15,5"
                    Margin="0,0,10,0"
                    Click="DeleteReaderButton_Click"
                    IsEnabled="False"/>

            <Button x:Name="ReaderTicketButton" 
                    Content="Читательский билет" 
                    Padding="15,5"
                    Margin="0,0,10,0"
                    Click="ReaderTicketButton_Click"
                    IsEnabled="False"/>

            <Button x:Name="CloseButton" 
                    Content="Закрыть" 
                    Padding="15,5"
                    Click="CloseButton_Click"/>
        </StackPanel>
    </Grid>
</Window>